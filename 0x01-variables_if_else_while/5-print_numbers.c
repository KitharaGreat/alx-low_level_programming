#include <stdio.h>

/**
 *main - this will print all single digits
 *
 *Return: will return 0
 */

int main(void)
{
	int a;

	for (a = 0 ; a > 10 ; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
