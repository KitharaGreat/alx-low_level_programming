#include <stdio.h>

/**
 * main - to print alphabets in lowercase
 *
 * Return: it returns 0
 */
int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar (i);
	putchar ('\n');
	return (0);

}
